# ELKBLEDOM LED Controller

This project is an LED controller based on the ESP8266 microcontroller. It allows you to control RGB LED strips using a command interface. The controller supports various settings and can be easily integrated into your smart home setup.